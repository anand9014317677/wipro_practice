# JavaMailingApp

Spring Boot Java Mail project with two APIs:

- POST /api/send-mail
- POST /api/send-mail-attachment

Configure Gmail App Password in src/main/resources/application.properties before running.
